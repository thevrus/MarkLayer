{
  "name": "@marklayer/fetcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --target=bun --minify --outfile dist/server.js",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@marklayer/types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
